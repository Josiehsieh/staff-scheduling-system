# ⚡ 快速部署指令 - 複製貼上即可

## 📋 完整指令清單

**請依序執行以下命令（在 PowerShell 中）：**

---

### 步驟 1：初始化 Git 並上傳

```powershell
# 進入專案目錄
cd C:\Users\josie\staff-scheduling-system

# 設定 Git 使用者資訊（第一次使用 Git 時需要）
git config --global user.name "Your Name"
git config --global user.email "your.email@gmail.com"

# 初始化 Git
git init

# 加入所有檔案
git add .

# 建立第一個 commit
git commit -m "Initial commit: 醫療排班管理系統"

# 設定主分支
git branch -M main

# 連結到 GitHub（請替換成您的 Repository URL）
git remote add origin https://github.com/YOUR-USERNAME/staff-scheduling-system.git

# 上傳到 GitHub
git push -u origin main
```

---

### 步驟 2：建立 GitHub Repository

**在執行上面的指令前，先在 GitHub 建立 Repository：**

1. 訪問：https://github.com/new
2. Repository name: `staff-scheduling-system`
3. Description: `醫療人員排班管理系統`
4. 選擇 Public 或 Private
5. **不要**勾選任何初始化選項
6. 點擊「Create repository」
7. 複製顯示的 Repository URL

---

### 步驟 3：啟用 GitHub Pages

**在 GitHub 網站上：**

1. 進入您的 Repository
2. Settings → Pages
3. Source: Deploy from a branch
4. Branch: main
5. Folder: / (root)
6. Save

等待 2-3 分鐘，您的網站就上線了！

網址會是：`https://YOUR-USERNAME.github.io/staff-scheduling-system/`

---

## 🔄 後續更新指令

當您修改程式碼後，執行以下指令更新：

```powershell
# 進入專案目錄
cd C:\Users\josie\staff-scheduling-system

# 加入所有修改
git add .

# 建立 commit（修改描述內容）
git commit -m "更新說明：描述您的修改"

# 上傳到 GitHub
git push
```

等待 2-3 分鐘，GitHub Pages 會自動更新。

---

## 🎯 更新 OAuth 設定

部署完成後，**必須**更新 Google OAuth 設定：

1. 前往：https://console.cloud.google.com/apis/credentials
2. 編輯您的 OAuth 用戶端 ID
3. 新增以下 URI（請替換 YOUR-USERNAME）：

**已授權的 JavaScript 來源：**
```
https://YOUR-USERNAME.github.io
```

**已授權的重新導向 URI：**
```
https://YOUR-USERNAME.github.io/staff-scheduling-system
https://YOUR-USERNAME.github.io/staff-scheduling-system/
https://YOUR-USERNAME.github.io/staff-scheduling-system/shift_management_system_sheets_full.html
```

4. 儲存

---

## ✅ 檢查清單

部署前確認：

```
□ 已建立 GitHub 帳號
□ 已在 GitHub 建立 Repository
□ 已複製 Repository URL
□ 已執行所有 Git 指令
□ 已啟用 GitHub Pages
□ 已更新 OAuth 重新導向 URI
□ 已測試網站可以開啟
□ 已測試登入功能
```

---

## 🆘 遇到問題？

### 錯誤：「Authentication failed」

使用 Personal Access Token：
1. https://github.com/settings/tokens
2. Generate new token (classic)
3. 勾選 `repo`
4. 複製 token
5. 推送時貼上 token（而非密碼）

### 錯誤：「remote origin already exists」

```powershell
git remote remove origin
git remote add origin https://github.com/YOUR-USERNAME/staff-scheduling-system.git
```

### 網站 404

1. 等待 5 分鐘
2. 確認 Pages 設定正確
3. 重新整理（Ctrl+F5）

---

**就這麼簡單！** 🎉


