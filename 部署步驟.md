# 🚀 部署到 GitHub Pages - 完整步驟

按照以下步驟，將您的排班系統部署到 GitHub Pages。

---

## 📋 前置準備

### 1. 確認已安裝 Git

在 PowerShell 中執行：
```powershell
git --version
```

如果沒有安裝，請從 https://git-scm.com/download/win 下載安裝。

### 2. 確認有 GitHub 帳號

如果沒有，請到 https://github.com 註冊。

---

## 🎯 步驟 1：建立 GitHub Repository

### 方法 1：在 GitHub 網站上建立

1. **登入 GitHub**
   - 訪問 https://github.com

2. **建立新 Repository**
   - 點擊右上角的「+」→「New repository」
   - Repository name: `staff-scheduling-system`
   - Description: 醫療人員排班管理系統
   - 選擇 Public 或 Private（兩者都可以用 GitHub Pages）
   - **不要**勾選「Add a README file」（我們已經有了）
   - 點擊「Create repository」

3. **複製 Repository URL**
   - 建立後會看到一個 URL，像是：
   ```
   https://github.com/your-username/staff-scheduling-system.git
   ```
   - 複製這個 URL

---

## 🎯 步驟 2：上傳專案到 GitHub

### 在 PowerShell 中執行以下命令

1. **進入專案目錄**
```powershell
cd C:\Users\josie\staff-scheduling-system
```

2. **初始化 Git（如果還沒有）**
```powershell
git init
```

3. **設定 Git 使用者資訊（如果是第一次使用）**
```powershell
git config --global user.name "Your Name"
git config --global user.email "your.email@gmail.com"
```

4. **將所有檔案加入 Git**
```powershell
git add .
```

5. **建立第一個 commit**
```powershell
git commit -m "Initial commit: 醫療排班管理系統"
```

6. **設定主分支名稱**
```powershell
git branch -M main
```

7. **連結到 GitHub Repository**
```powershell
# 將下面的 URL 替換成您的 Repository URL
git remote add origin https://github.com/your-username/staff-scheduling-system.git
```

8. **上傳到 GitHub**
```powershell
git push -u origin main
```

如果要求輸入帳號密碼：
- Username: 您的 GitHub 用戶名
- Password: 使用 Personal Access Token（不是密碼）
  - 前往 https://github.com/settings/tokens
  - 點擊「Generate new token (classic)」
  - 勾選 `repo` 權限
  - 複製產生的 token 並貼上

---

## 🎯 步驟 3：啟用 GitHub Pages

1. **前往 Repository 設定**
   - 在 GitHub 上開啟您的 repository
   - 點擊「Settings」（設定）

2. **找到 Pages 設定**
   - 在左側選單中找到「Pages」

3. **配置 GitHub Pages**
   - **Source**（來源）：選擇「Deploy from a branch」
   - **Branch**（分支）：選擇「main」
   - **Folder**（資料夾）：選擇「/ (root)」
   - 點擊「Save」

4. **等待部署**
   - 頁面會顯示：「Your site is live at https://your-username.github.io/staff-scheduling-system/」
   - 等待 2-3 分鐘讓 GitHub 建置網站

5. **測試網站**
   - 訪問：`https://your-username.github.io/staff-scheduling-system/`
   - 應該會自動跳轉到排班系統

---

## 🎯 步驟 4：更新 Google OAuth 設定

**重要！** 您的網站現在有了新的網址，需要更新 OAuth 設定。

1. **前往 Google Cloud Console**
   ```
   https://console.cloud.google.com/apis/credentials
   ```

2. **編輯您的 OAuth 用戶端 ID**
   - 找到您之前建立的 OAuth 2.0 用戶端 ID
   - 點擊編輯（鉛筆圖示）

3. **保留 localhost 設定（開發用）**
   
   已授權的 JavaScript 來源：
   ```
   http://localhost:8000  （保留）
   ```
   
   已授權的重新導向 URI：
   ```
   http://localhost:8000  （保留）
   http://localhost:8000/  （保留）
   http://localhost:8000/shift_management_system_sheets_full.html  （保留）
   ```

4. **新增 GitHub Pages 設定（正式用）**
   
   點擊「+ 新增 URI」，新增以下項目：
   
   已授權的 JavaScript 來源：
   ```
   https://your-username.github.io
   ```
   
   已授權的重新導向 URI：
   ```
   https://your-username.github.io/staff-scheduling-system
   https://your-username.github.io/staff-scheduling-system/
   https://your-username.github.io/staff-scheduling-system/shift_management_system_sheets_full.html
   ```
   
   **注意**：將 `your-username` 替換成您的 GitHub 用戶名

5. **儲存**

---

## 🎯 步驟 5：測試 GitHub Pages 上的系統

1. **開啟您的網站**
   ```
   https://your-username.github.io/staff-scheduling-system/
   ```

2. **進入設定頁面**
   - 點擊「⚙️ 設定」
   - 確認用戶端 ID 已填入（使用 localStorage 儲存）

3. **登入 Google**
   - 點擊「🔐 登入 Google」
   - 應該能正常登入
   - 如果出現錯誤，檢查 OAuth 設定是否正確

4. **測試所有功能**
   - ✅ 新增排班
   - ✅ 編輯排班
   - ✅ 刪除排班
   - ✅ 上傳到 Google Calendar
   - ✅ 同步到 Google Sheets
   - ✅ 匯出 Excel

---

## 🔄 後續更新

當您修改程式碼後，如何更新到 GitHub Pages：

1. **在專案目錄中**
```powershell
cd C:\Users\josie\staff-scheduling-system
```

2. **查看修改的檔案**
```powershell
git status
```

3. **加入修改的檔案**
```powershell
git add .
```

4. **建立 commit**
```powershell
git commit -m "描述您做了什麼修改"
```

5. **上傳到 GitHub**
```powershell
git push
```

6. **等待 2-3 分鐘**
   - GitHub Pages 會自動重新部署
   - 重新整理網頁就能看到更新

---

## 📱 分享給其他人使用

### 分享網址

直接分享：
```
https://your-username.github.io/staff-scheduling-system/
```

### 其他人如何使用

1. **他們需要建立自己的 Google Cloud 專案**
   - 按照 [API設定快速參考.md](./API設定快速參考.md) 設定
   - **OAuth 重新導向 URI 使用您的 GitHub Pages 網址**

2. **在系統設定中輸入自己的用戶端 ID**
   - 每個人使用自己的 Google Cloud 專案
   - 資料儲存在各自的 Google 帳號中

3. **登入並授權**
   - 使用自己的 Google 帳號登入
   - 授權 Sheets 和 Calendar 權限

---

## ⚠️ 常見問題

### Q：推送到 GitHub 時出現「Authentication failed」

**A：需要使用 Personal Access Token**

1. 前往 https://github.com/settings/tokens
2. 點擊「Generate new token (classic)」
3. 勾選 `repo` 權限
4. 複製 token
5. 在 PowerShell 推送時：
   - Username: 您的 GitHub 用戶名
   - Password: 貼上剛才複製的 token

### Q：GitHub Pages 顯示 404

**A：可能原因：**
1. 等待 2-3 分鐘讓 GitHub 建置
2. 確認 Pages 設定中選擇了正確的分支（main）
3. 確認資料夾選擇了「/ (root)」
4. 重新整理頁面（按 Ctrl+F5）

### Q：網站可以開啟但無法登入

**A：檢查 OAuth 設定：**
1. 確認已在 Google Cloud Console 中新增 GitHub Pages 的 URI
2. 確認 URI 格式正確（https://，結尾的斜線等）
3. 按 F12 查看 Console 的錯誤訊息
4. 參考 [登入問題故障排除.md](./登入問題故障排除.md)

### Q：想要改成私有 Repository

**A：可以，GitHub Pages 支援私有 Repository**
1. Repository → Settings → Danger Zone
2. 點擊「Change visibility」
3. 選擇「Make private」
4. GitHub Pages 功能不受影響

---

## 🎉 完成！

恭喜您成功部署到 GitHub Pages！

**您的網站網址：**
```
https://your-username.github.io/staff-scheduling-system/
```

**後續步驟：**
- ✅ 測試所有功能
- ✅ 分享給同事使用
- ✅ 根據需求繼續開發
- ✅ 定期更新到 GitHub

---

## 📚 相關文檔

- [README.md](./README.md) - 系統總覽
- [API設定快速參考.md](./API設定快速參考.md) - OAuth 設定
- [登入問題故障排除.md](./登入問題故障排除.md) - 疑難排解
- [部署到 GitHub Pages 說明.md](./部署到%20GitHub%20Pages%20說明.md) - 詳細說明

---

**祝您使用愉快！** 🎉


